# Plagiarism-Checker
Plagiarism Checker script provides a simple GUI application to compare the similarity between the contents of two text files. It uses the Tkinter library for the GUI and difflib library for comparing the text similarity.
